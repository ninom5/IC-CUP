{
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:generate": "yarn workspace api run db:generate",
    "db:migrate": "yarn workspace api run db:migrate",
    "db:seed": "yarn workspace api run db:seed",
    "db:reset": "yarn workspace api run db:reset",
    "start": "node apps/api/dist/src/main"
  },
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "yarn@1.22.22"
}
