import vehicleAddedIcon from "assets/images/vehicleAddedIcon.svg";

export const VehicleAddedMessage = () => {
  return (
    <section>
      <h1>Uspješno si dodao svoje vozilo</h1>
      <img src={vehicleAddedIcon} />
    </section>
  );
};
